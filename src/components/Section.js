import React from 'react'
import './sectionStyle/Section.css'
const Section = (props) => {
    return (
        <section className = "section">
        just want to write some thing
        </section>
    )
}

export default Section
